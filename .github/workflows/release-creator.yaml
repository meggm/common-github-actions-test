# Copyright (c) 2024 Dell Inc., or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#  http://www.apache.org/licenses/LICENSE-2.0

# This workflow is a reusable one for creating tag and release.
name: Tag and Release Creator

# Invocable as a reusable workflow
on:
  workflow_call:
    inputs:
      version:
        description: 'Version to release (major, minor, patch), ex: 1.0.0'
        required: true
        type: string
#      gpg_private_key:
#        required: true
#        type: string
#      gpg_passphrase:
#        required: false
#        type: string        

jobs:
  tag-and-release:
    name: Create tag and release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch the full history including tags

      - name: Get latest release
        run: |
          latest_version=$(git describe --tags $(git rev-list --tags --max-count=1))
          echo "MY_VAR=${latest_version}" >> $GITHUB_ENV            
          echo "latest_version=${latest_version}"

      - name: Increment version
        env:
          MY_VAR: ${{ env.MY_VAR }}
        run: |
          current_version=$MY_VAR
          current_version=(${current_version#v}) # remove 'v' prefix from $current_version"
          
          IFS='.' read -r -a version_parts <<< "$current_version"              
          
          if "${{ inputs.version == 'major' }}"; then
              # major version bump up
              version_parts[0]=$(expr ${version_parts[0]} + 1)
              new_version="${version_parts[0]}.0.0"
          fi
          
          if "${{ inputs.version == 'minor' }}"; then
              # minor version bump up
              version_parts[1]=$(expr ${version_parts[1]} + 1)
              new_version="${version_parts[0]}.${version_parts[1]}.0"
          fi
          
          if "${{ inputs.version == 'patch' }}"; then
              # patch version bump up
              version_parts[2]=$(expr ${version_parts[2]} + 1)
              new_version="${version_parts[0]}.${version_parts[1]}.${version_parts[2]}"
          fi
          echo "New version to be released:v$new_version"
          echo "REL_VERSION=v${new_version}" >> $GITHUB_ENV
          
      - name: Decode and sanitize GPG private key
        id: sanitize-gpg-key
        run: |
          # Decode the base64-encoded GPG private key stored in GitHub Secrets
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | base64 --decode | tee gpg_private_key.asc

          # Remove any extra spaces or line breaks (optional)
          sed -i 's/\r//g' gpg_private_key.asc  # Remove carriage return (if any)

          # The sanitized key is saved as an output so it can be used in the next step
          sanitized_key=$(cat gpg_private_key.asc)

          # Clean up by removing the file (optional)
          rm -f gpg_private_key.asc

          # Set the sanitized key as an output variable
          echo "::set-output name=gpg_private_key::$sanitized_key"

      - name: Import GPG key
        uses: crazy-max/ghaction-import-gpg@v6
        with:
          gpg_private_key: ${{ steps.sanitize-gpg-key.outputs.gpg_private_key }}
          passphrase: ${{ secrets.GPG_PASSPHRASE }}
          git_user_signingkey: true
          git_commit_gpgsign: true
          git_tag_gpgsign: true
          git_config_global: true
          
      - name: Create new tag
        run: |
          git tag -s -a v${{ github.event.actions.set-version.outputs.version }} -m "Release v${{ github.event.inputs.version }}"
          git push origin v${{ github.event.inputs.version }}

      - name: Create Release
        id: release_notes
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.event.inputs.version }}
          name: Release v${{ github.event.inputs.version }}
          draft: true
          prerelease: false
          generate_release_notes: true
          make_latest: true
          body: |
            ## Documentation
            [General Documentation](https://dell.github.io/csm-docs/docs/)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create release branch
        run: |
          git checkout -b release/v${{ github.event.inputs.version }}
          git push origin release/v${{ github.event.inputs.version }}
